# 上线部署指南

## 前置准备

### 必须手动修改的配置

**1. `.env` — 安全配置**

```bash
# 生成强随机 API Key
python -c "import secrets; print(secrets.token_urlsafe(32))"

# .env 关键配置项
ENVIRONMENT=production
ENABLE_AUTH=true
API_KEY=<生成的密钥>
CORS_ORIGINS=https://your-domain.com
```

**2. `economic-news-webapp/.env` — API 地址**

```bash
REACT_APP_API_URL=https://api.your-domain.com
```

**3. Nginx 配置**

编辑 `deploy/nginx.conf`，将所有 `your-domain.com` 替换为实际域名。

---

## 服务器环境要求

| 组件 | 版本 | 用途 |
|------|------|------|
| Ubuntu 20.04+ / CentOS 7+ | — | 操作系统 |
| Python | 3.10+ | 后端运行 |
| Node.js | 18+ | 前端构建 |
| Nginx | 1.18+ | 反向代理 + HTTPS |
| SQLite | 3.35+ (系统自带) | 数据库 |
| Chromium | (Playwright 自动安装) | 搜狗微信/雪球爬虫 |

---

## 部署方式选择

| 方式 | 适用场景 | 命令 |
|------|----------|------|
| **Docker Compose（推荐）** | 快速部署 / 环境隔离 / 一键启停 | `docker-compose up -d` |
| **systemd 手动部署** | 裸金属 / VPS / 精细控制 | 详见下方步骤 |

---

## 方式 A：Docker Compose 部署（推荐）

```bash
# 1. 安装 Docker
curl -fsSL https://get.docker.com | sh
sudo apt install docker-compose -y

# 2. 上传项目
cd /opt/flowcapital

# 3. 构建前端
cd economic-news-webapp && npm install && npm run build && cd ..

# 4. 一键启动
docker-compose up -d

# 5. 查看状态
docker-compose ps
docker-compose logs -f
```

**容器架构：**
```
flowcapital-nginx (80/443) → flowcapital-api (8001)
flowcapital-scheduler → 每15min自动爬取
```

---

## 方式 B：systemd 手动部署

### 第一步：环境安装

```bash
# 更新系统
sudo apt update && sudo apt upgrade -y

# 安装依赖
sudo apt install -y python3 python3-pip python3-venv nodejs npm nginx certbot python3-certbot-nginx

# 安装 Chromium（Playwright 依赖）
npx playwright install chromium
# 或: python -m playwright install chromium

# 创建项目目录
sudo mkdir -p /opt/flowcapital
sudo chown $USER:$USER /opt/flowcapital
```

---

### 第二步：部署后端

```bash
# 上传项目到服务器
cd /opt/flowcapital
# git clone ... 或 rsync -avz ...

# 安装 Python 依赖
cd scripts
pip install -r requirements.txt
cd ..

# 创建数据目录
mkdir -p data logs backup

# 初始化数据库（首次运行自动创建）
python -c "from backend.database.db_manager import db; print('DB OK')"

# 构建 Whoosh 搜索索引
python -c "
from backend.database.db_manager import db
from backend.analyzer.search_engine import rebuild_index
news, _ = db.get_latest_news(limit=9999)
rebuild_index(news)
"

# 注册 systemd 服务
sudo cp deploy/backend.service /etc/systemd/system/flowcapital-api.service
sudo cp deploy/scheduler.service /etc/systemd/system/flowcapital-scheduler.service
sudo systemctl daemon-reload
sudo systemctl enable flowcapital-api flowcapital-scheduler
sudo systemctl start flowcapital-api flowcapital-scheduler
```

---

### 第三步：部署前端

```bash
cd /opt/flowcapital/economic-news-webapp

# 安装依赖
npm install

# 生产构建
npm run build

# 部署到 Nginx 静态目录
sudo mkdir -p /var/www/flowcapital
sudo cp -r build/* /var/www/flowcapital/
sudo chown -R www-data:www-data /var/www/flowcapital
```

---

### 第四步：配置 Nginx

```bash
# 复制 Nginx 配置
sudo cp /opt/flowcapital/deploy/nginx.conf /etc/nginx/sites-available/flowcapital
sudo ln -s /etc/nginx/sites-available/flowcapital /etc/nginx/sites-enabled/
sudo rm -f /etc/nginx/sites-enabled/default

# 测试配置
sudo nginx -t

# 重载
sudo systemctl reload nginx
```

---

### 第五步：配置 HTTPS

```bash
# 使用 Let's Encrypt 免费证书
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# 证书自动续期（已自动配置）
sudo certbot renew --dry-run
```

---

### 第六步：配置自动备份

```bash
# 复制备份脚本
sudo cp /opt/flowcapital/deploy/backup.sh /opt/flowcapital/scripts/

# 添加 cron 任务（每天凌晨 3 点备份）
crontab -e
# 添加以下行:
0 3 * * * /opt/flowcapital/scripts/backup.sh >> /opt/flowcapital/logs/backup.log 2>&1

# 保留最近 7 天的备份
# (已在 backup.sh 中配置)
```

---

## 验证部署

```bash
# 检查服务状态
sudo systemctl status flowcapital-api
sudo systemctl status flowcapital-scheduler

# 检查 API
curl https://api.your-domain.com/api/health

# 检查前端
curl https://your-domain.com/

# 查看日志
sudo journalctl -u flowcapital-api -f
sudo journalctl -u flowcapital-scheduler -f
tail -f /opt/flowcapital/logs/scheduler.log
```

---

## 前端写操作认证

对于需要认证的接口（留言、翻译、重建索引等），前端需在请求头中携带 API Key：

```javascript
headers: { 'X-API-Key': '<你的API密钥>' }
```

当前写操作受保护的端点：
- `POST /api/guest/message` — 发表留言
- `POST /api/news/translate` — 翻译新闻
- `POST /api/news/rebuild-index` — 重建索引
- `POST /api/graph-rag/build-graph` — 构建知识图谱

---

## 目录结构（生产环境）

```
/opt/flowcapital/
├── backend/              # 后端代码
├── scripts/              # 脚本 + 依赖
│   ├── scheduler.py      # 调度器
│   ├── run_project.py    # 启动器
│   └── backup.sh         # 备份脚本
├── graph_rag/            # GraphRAG 模块
├── data/
│   ├── sqlite.db         # 数据库
│   └── whoosh_index/     # 搜索索引
├── logs/                 # 日志
├── backup/               # 备份目录
├── deploy/               # 部署配置
│   ├── nginx.conf        # HTTPS + 反向代理
│   ├── backend.service   # API systemd
│   ├── scheduler.service # 调度器 systemd
│   └── backup.sh         # 备份脚本
├── economic-news-webapp/ # 前端（仅构建用）
├── docker-compose.yml    # Docker 部署
├── Dockerfile.api        # API 镜像
└── .env                  # 环境变量（生产配置）

/var/www/flowcapital/     # 前端静态文件 (build/)
```

---

## 性能调优

### Nginx 层
- `gzip_static on` — 预压缩的静态文件
- `expires 30d` — 静态资源强缓存
- `limit_req_zone` — 请求频率限制

### 应用层
- FastAPI: Linux 下使用 4 worker（`--workers 4`），Windows 仅支持单 worker
- Redis: 非必须，自动降级到内存缓存
- 调度器: 每 15 分钟爬取一次

### 监控建议

**UptimeRobot（免费）** — https://uptimerobot.com
```
监控 URL: https://your-domain.com/api/health
监控间隔: 5 分钟
告警方式: Email / Slack / Webhook
```
健康检查返回示例：
```json
{"status":"ok","service":"经济新闻分析系统","version":"1.0.0",
 "metrics":{"news_count":1461},
 "checks":{"sqlite":"connected","redis":"not_configured","whoosh":"ready"}}
```

**服务器监控**
```bash
# CPU / 内存 / 磁盘
htop
df -h

# 服务状态
sudo systemctl status flowcapital-api flowcapital-scheduler

# 实时日志
tail -f /opt/flowcapital/logs/scheduler.log
sudo journalctl -u flowcapital-api -f
```

---

## 升级部署

```bash
# 1. 拉取最新代码
cd /opt/flowcapital && git pull

# 2. 更新 Python 依赖
pip install -r scripts/requirements.txt

# 3. 重新构建前端
cd economic-news-webapp && npm install && npm run build
sudo cp -r build/* /var/www/flowcapital/

# 4. 重启服务
sudo systemctl restart flowcapital-api flowcapital-scheduler

# 5. 清除缓存
python -c "from backend.database.redis_client import redis_client; redis_client.flushdb()"
```

---

## 前端容错

前端已内置 `ErrorBoundary` 组件。当页面 JS 崩溃时，自动显示错误页 + "返回首页"/"刷新页面" 按钮，而非白屏。

---

## 故障排除

| 问题 | 检查 |
|------|------|
| 502 Bad Gateway | `sudo systemctl status flowcapital-api` 确认后端运行 |
| 前端白屏 / 黑屏 | 查看浏览器 Console 错误；ErrorBoundary 会显示友好页 |
| 首页加载慢 | 检查 Redis 是否运行（非必须），缓存是否命中 |
| 爬虫不运行 | `sudo journalctl -u flowcapital-scheduler -f` 查看调度器日志 |
| 搜索无结果 | 重建索引: `curl -X POST https://域名/api/news/rebuild-index -H 'X-API-Key: <key>'` |
| SSL 证书过期 | `sudo certbot renew` 手动续期 |
| 数据库损坏 | 从 `backup/` 恢复最近的 `sqlite.db` |
| Docker 容器异常 | `docker-compose logs api` / `docker-compose restart` |

---

## 回滚步骤

```bash
# systemd 方式
sudo systemctl stop flowcapital-api flowcapital-scheduler
cp backup/sqlite_YYYYMMDD_HHMMSS.db data/sqlite.db
cd /opt/flowcapital && git checkout <previous-commit>
sudo systemctl start flowcapital-api flowcapital-scheduler

# Docker 方式
docker-compose down
cp backup/sqlite_YYYYMMDD_HHMMSS.db data/sqlite.db
docker-compose up -d
```
